import { useState, useEffect } from 'react';
import api from '../services/api';

export function IpStatus() {
  
  
  return { data, 
    fetchData, 
    configBot, 
    historialIps, 
    IpStatus,
    registrarBot,
    loading };
}